'use client';

import { Text } from '@medusajs/ui';

const Error = () => {
  return (
    <div className="flex flex-col gap-2">
      <Text>잘못된 요청입니다.</Text>
    </div>
  );
};

export default Error;
